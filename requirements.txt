# Core numerical stack (pin to numpy<2 to satisfy mediapipe and avoid mixed-state downgrades)
numpy>=1.26.0,<2.0.0
scipy
pandas

# Computer vision
opencv-python
mediapipe==0.10.21
matplotlib
scikit-image

# Deep learning / ASR
# Use PyTorch version before 2.6 to avoid weights_only security issues
torch>=2.0.0,<2.6.0
transformers==4.44.2
accelerate
# Datasets with audio extras used by transformers pipelines
datasets[audio]

# Audio processing
librosa
opensmile
ffmpeg-python
imageio-ffmpeg
pyannote-audio

# Object detection
yolov5

# GUI
wxPython

# Utilities
requests
python-dotenv
