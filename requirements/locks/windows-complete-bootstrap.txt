# Resolver-only constraints for the existing Python 3.10 Complete profile.
# They are intentionally not project dependencies; the generated hash lock is
# the release input. These keep pyannote 3.2 on its NumPy-1-compatible line.
pyannote.core==5.0.0
pyannote.database==5.1.3
pyannote.metrics==3.2.1
pyannote.pipeline==3.0.1
huggingface-hub==0.24.7
